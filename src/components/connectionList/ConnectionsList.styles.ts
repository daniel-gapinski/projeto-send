import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    noConnections: {
        color: "#6B7280",
        textAlign: "center",
        marginTop: "10px",
    },
    listItem: {
        borderBottom: "1px solid #E5E7EB",
        padding: "10px 20px",
    },
    link: {
        textDecoration: "none",
    },
});
