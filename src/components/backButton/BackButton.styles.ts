import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    backButton: {
        fontWeight: 600,
        fontSize: "1.25rem",
        color: "#4B5563",
        marginBottom: 24,
        display: "flex",
        alignItems: "center",
        cursor: "pointer",
    },
    icon: {
        marginRight: 8,
    },
});
