import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    noContacts: {
        color: "#6B7280",
        textAlign: "center",
    },
    listItem: {
        borderBottom: "1px solid #E5E7EB",
    },
    icon: {
        color: "#3B82F6",
    },
});
