import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    backButton: {
        marginBottom: 24,
    },
    formControl: {
        marginBottom: 24,
        marginTop: 24,
    },
    list: {
        marginBottom: 24,
    },
    button: {
        marginTop: 24,
        marginBottom: 32,
    },
    mt: {
        marginTop: 5,
    }
});
