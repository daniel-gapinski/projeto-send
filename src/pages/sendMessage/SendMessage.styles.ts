import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    header: {
        fontWeight: 600,
        color: "#4A4A4A",
        marginBottom: 24,
        display: "flex",
        alignItems: "center",
        cursor: "pointer",
    },
    icon: {
        marginRight: 8,
    },
    input: {
        marginBottom: 16,
        marginTop: 16,
    },
    formControl: {
        marginBottom: 16,
    },
    divider: {
        marginBottom: 16,
    },
});
